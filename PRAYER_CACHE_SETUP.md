# نظام تخزين أوقات الصلاة الذكي

## كيفية العمل

تم تطوير نظام ذكي لتخزين أوقات الصلاة في **local storage** وتحديثها **مرة واحدة يوميا فقط**:

### المراحل:

1. **عند فتح الموقع أول مرة في اليوم:**
   - يتحقق التطبيق من وجود بيانات محفوظة في local storage
   - إذا لم توجد بيانات أو كانت قديمة (من يوم سابق)، يستدعي API
   - يحفظ البيانات الجديدة مع تاريخ اليوم الحالي

2. **عند فتح الموقع مرات إضافية في نفس اليوم:**
   - يستخدم البيانات المحفوظة بدون استدعاء API
   - ترتيب أسرع وأقل استهلاكاً للبيانات

3. **عند تغيير الدولة أو المدينة:**
   - يتحقق من الـ cache للدولة والمدينة الجديدة
   - إذا كانت موجودة لليوم الحالي، يستخدمها
   - وإلا، يستدعي API ويحفظ البيانات الجديدة

### التغييرات المنفذة:

**الملف الجديد:** `src/utils/prayerCache.js`
- دوال للتحكم في تخزين واسترجاع البيانات
- التحقق التلقائي من صحة التاريخ

**الملف المعدل:** `src/components/PrayerContext.jsx`
- إضافة التحقق من الـ cache قبل استدعاء API
- حفظ البيانات الجديدة تلقائياً

## الفوائد

✅ تقليل استدعاءات API بنسبة كبيرة  
✅ تحميل أسرع للبيانات المحفوظة  
✅ توفير نطاق الإنترنت  
✅ تحديث يومي تلقائي بدون تدخل من المستخدم  

## كيفية الاختبار

1. افتح الموقع لأول مرة في اليوم - سيستدعي API
2. افتح أدوات المطور (F12) وانتقل إلى **Application > Local Storage**
3. ابحث عن `prayerTimesCache` - ستجد البيانات المحفوظة
4. أغلق الموقع وافتحه مرة أخرى - سيستخدم البيانات المحفوظة بدون API
5. غير الدولة أو المدينة - سيكرر نفس العملية للموقع الجديد

## مسح البيانات يدويا

إذا أردت مسح الـ cache يدويا:
```javascript
// في console
localStorage.removeItem('prayerTimesCache');
```

أو يمكن استخدام الدالة من الكود:
```javascript
import { clearPrayerCache } from 'src/utils/prayerCache';
clearPrayerCache();
```
